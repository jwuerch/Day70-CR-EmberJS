import { favoriteCount } from 'day65-cr-message-board/helpers/favorite-count';
import { module, test } from 'qunit';

module('Unit | Helper | favorite count');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = favoriteCount([42]);
  assert.ok(result);
});
