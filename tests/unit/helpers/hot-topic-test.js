import { hotTopic } from 'day65-cr-message-board/helpers/hot-topic';
import { module, test } from 'qunit';

module('Unit | Helper | hot topic');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hotTopic([42]);
  assert.ok(result);
});
