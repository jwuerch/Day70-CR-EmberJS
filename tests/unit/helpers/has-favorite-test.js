import { hasFavorite } from 'day65-cr-message-board/helpers/has-favorite';
import { module, test } from 'qunit';

module('Unit | Helper | has favorite');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hasFavorite([42]);
  assert.ok(result);
});
